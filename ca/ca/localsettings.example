# This is an example localsettings.py file for django-ca. Also see
#   http://django-ca.readthedocs.org/en/latest/settings.html
# for more information.

import os
from OpenSSL import crypto

# BASEDIR is the directory above this file, where your manage.py file is located.
BASE_DIR = os.path.dirname(os.path.dirname(__file__))

# configure your database here:
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',  # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': '/etc/fsinf-ca/db.sqlite3',  # Or path to database file if using sqlite3.
        # The following settings are not used with sqlite3:
        'USER': '',
        'PASSWORD': '',
        'HOST': '',                      # Empty for localhost through domain sockets or '127.0.0.1' for localhost through TCP.
        'PORT': '',                      # Set to empty string for default.
    }
}

##################
### CA options ###
##################
# The directory where your CA files are located
#CA_DIR = os.path.join(BASE_DIR, 'files')

# The key type to use for create the ca private key
#CA_KEY_TYPE = crypto.TYPE_RSA

###########################
### Certificate options ###
###########################
# The digest algorithm used for signing certificates, use sha256 with older (pre 2010) clients.
#DIGEST_ALGORITHM = 'sha512'

# Bits used for private keys
#CA_BITSIZE = 4096

# Any URL you have for your CA
#CA_ISSUER_ALT_NAME = None

# List of URLs where clients can fetch a CRL. The CRL can be generated with the "manage.py crl"
# command. You have to take care of hosting the CRL.
#CA_CRL_DISTRIBUTION_POINTS = []

# URL of a OCSP service. Do not set this unless you actually intend to run an OCSP service, as
# OCSP-enabled clients will reject a connection if the OCSP-service is not available.
#CA_OCSP = None

# URL of the CA root certificate
#CA_ISSUER = None

# List of default key usage attributes for new certificates. See
#   https://www.openssl.org/docs/apps/x509v3_config.html#key_usage_
# for more information.
#CA_KEY_USAGE = (
#    'keyEncipherment',
#    'keyAgreement',
#    'digitalSignature',
#)

# List of default extended key usage attributes for new certificates. See
#   https://www.openssl.org/docs/apps/x509v3_config.html#extended_key_usage_
# for more information.
#CA_EXT_KEY_USAGE = (
#    'serverAuth',
#)
